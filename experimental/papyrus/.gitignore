# Nix build outputs
result
result-*

# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
client/dist/
*.tsbuildinfo

# Terminal fonts, generated from nix (flake `terminalFonts` / scripts/sync-terminal-fonts.sh).
# Bundled into the build; never committed (Nerd Font TTFs are ~10 MB each).
client/public/fonts/

# Environment
.env
.env.local

# State
.openui/

# OS
.DS_Store
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp
*.tmp
.cache/
